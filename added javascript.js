function addJavaScript(str) {
    return "JavaScript " + str;
}

const originalString = "is a programing language.";
const newString = addJavaScript(originalString);

console.log(newString); 
